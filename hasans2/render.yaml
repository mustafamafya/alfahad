services:
  - type: web
    name: alfahad-django
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn hasans2.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: hasans2.settings
      - key: SECRET_KEY
        value: 'django-insecure-asb&!j(7(sp^ktuvwrc=bsib2mg6%egxlx93_@cpexh!*^7#sd'
      - key: ALLOWED_HOSTS
        value: alfahad.onrender.com